import 'package:flutter/material.dart';

class WordleTextField extends StatelessWidget {
  const WordleTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
